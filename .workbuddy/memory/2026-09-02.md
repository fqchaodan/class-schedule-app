# 2026-09-02

## UI 全面去渐变化 + tabbar 去激活色
- 背景：用户反馈"UI 丑"，要求优化样式并去掉 tabbar 激活色。
- tabbar（index.vue）：激活色 indigo(#6366f1) → 中性 #111827（+font-medium 字重区分），删除激活态顶部渐变指示条；config.ts 与 pages.json 的 selectedColor 同步为 #111827。
- NavBar.vue：indigo→blue 渐变 → 白底 + border-b border-gray-100，标题/返回按钮改深灰。
- 首页/统计页头部工具条：渐变 → 白底 + 细分割线，去掉所有 `!text-white` 按钮覆写。
- 统计页：汇总卡头部渐变条 → bg-gray-50 素色条；明细进度条渐变 → 纯 bg-indigo-500。
- DragFab：渐变 → 纯 bg-indigo-500（shadow-float 光晕保留）。
- CourseCard：去掉外层 border border-gray-100（纯阴影卡更干净），保留左侧 border-l-4 状态色条与渐变头像。
- uno.config.ts：删除已无引用的 gradient-primary / gradient-primary-br shortcut（全项目 grep 确认）。
- 视觉规范已同步更新至 MEMORY.md。

## 二次调整：去渐变后"太素"，改低饱和染色方案（web-design-guidelines skill 审查驱动）
- 头部（首页/统计）：纯白 → `bg-gradient-to-b from-indigo-50 to-white` 极浅染色渐变，保留视觉温度。
- 统计页汇总卡头部：gray-50 → indigo-50/80 + border-indigo-100 + indigo 文案（主色小面积点缀）。
- 统计三指标数字加 tabular-nums（等宽数字，skill 排版规则）。
- 首页周视图空状态：每天一个 wd-empty(60px) → 紧凑虚线空槽（border-dashed），消除大片空白。
- 首页日期标签：indigo primary 只给"今天"，有课日不再全变 primary；删掉未用的 dayHasCourse。
- 经验：用户对视觉的平衡点 = 不要大面积品牌渐变，也不要全白寡淡；用 indigo-50 级浅染色 + 小面积主色点缀。
