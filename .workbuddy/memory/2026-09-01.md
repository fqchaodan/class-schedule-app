# 2026-09-01

## 全站样式统一优化（不改功能）
- uno.config.ts 新增设计令牌 shortcuts：`card`（白卡+细边框+shadow-card 双层柔和阴影）、`gradient-primary`（横向 to-r）、`gradient-primary-br`（斜向头像/FAB）、文字层级 `txt-title/txt-heading/txt-body/txt-sub/txt-aux/txt-fee`、`section-label`、`status-dot`；rules 新增 `shadow-card`、`shadow-float`；theme primary fallback 改 `#6366f1`。
- src/style/index.scss 覆盖 wot-ui `--wot-primary-1~10` 为 indigo 色阶（tag/button/segmented 主色全统一 indigo）。
- 新建 src/utils/avatar.ts：`avatarGradient(title)` 按姓名 hash 取 6 组渐变（safelist 已含），CourseCard/detail/recycle/templates 四处头像同一学生同色。
- 渐变全站统一 to-r（顶部工具条/NavBar/统计卡头），头像/FAB 用 to-br；删除 via-indigo 混用。
- tabbar 激活色 `#1C64FD` → `var(--wot-primary-6, #6366f1)`；tabbar/config.ts 与 src/pages.json 的 selectedColor `#018d71` → `#6366f1`；角标 `#f56c6c` → red-500。
- settings 页图标色系收敛（indigo/sky/red/violet/gray 语义色）；recycle 卡片加 hash 头像；templates 页课程/周期两种卡片结构对齐（都带头像+card）；ConflictDialog 容器用卡片规范。
- 删除确认弹窗 confirmColor 统一 `#ef4444`（uni API 需 hex，保留）。
- `npx pnpm@10 type-check` 通过。
